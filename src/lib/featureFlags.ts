/**
 * Smart home / device status strip. HomeStatusWidget stays in the codebase
 * but is hidden when false. Quick actions are always shown (non-smart-home).
 */
export const HOME_STATUS_WIDGET_ENABLED = false
